import React from "react";

const TestimonialRotator = ({ quote }) => {
  return (
    <p className="relative z-10 mt-16 text-center text-blue-700 text-lg sm:text-xl md:text-2xl italic font-light px-4 max-w-3xl mx-auto">
      {quote}
    </p>
  );
};

export default TestimonialRotator;
